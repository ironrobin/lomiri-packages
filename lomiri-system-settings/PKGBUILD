# Maintainer: Ivan Semkin (ivan at semkin dot ru)

pkgname=lomiri-system-settings
_pkgname=lomiri-system-settings
pkgver=1.3.2
pkgrel=2
pkgdesc='System Settings application for Unity8'
url='https://gitlab.com/ubports/development/core/lomiri-system-settings'
arch=(x86_64 i686 armv7h aarch64)
license=(GPL3)
conflicts=(system-settings)
provides=(system-settings)
# No content-hub-git needed for mobile
depends=(click-git gnome-desktop lomiri-app-launch-git geonames libqtdbusmock-git lomiri-ui-toolkit-git gsettings-qt accountsservice upower suru-icon-theme-git qmenumodel libqofono-qt5)
makedepends=(git cmake intltool)
checkdepends=(xorg-server-xvfb)
source=('https://gitlab.com/ubports/development/core/lomiri-system-settings/-/archive/1.3.2/lomiri-system-settings-1.3.2.tar.gz'
        '2011_build-without-trust-store.patch'
)
sha256sums=('SKIP'
            'SKIP'
)

BUILDENV+=('!check') # Some tests fail

# pkgver() {
#   cd ${_pkgname}
#   echo "r$(git rev-list --count HEAD).$(git describe --always)"
# }

prepare() {
  cd ${_pkgname}-${pkgver}

  patch -Np1 -i "${srcdir}/2011_build-without-trust-store.patch"
}

build() {
  cd ${_pkgname}-${pkgver}
  mkdir -p build
  cd build
  cmake -DCMAKE_INSTALL_PREFIX:PATH=/usr -DCMAKE_INSTALL_LIBDIR="lib/" -DENABLE_TESTS=off ..
  make
}

# check() {
#   cd ${_pkgname}-${pkgver}/build
#   make ARGS+="--output-on-failure" test
# }

package() {
  cd ${_pkgname}-${pkgver}/build
  make DESTDIR="${pkgdir}/" install
}
# vim:set ts=2 sw=2 et:
