# Maintainer: Ivan Semkin (ivan at semkin dot ru)

pkgbase=lomiri-url-dispatcher
pkgname=(lomiri-url-dispatcher lomiri-url-dispatcher-gui)
_pkgname=lomiri-url-dispatcher
pkgver=0.1.4
pkgrel=3
pkgdesc='Service to allow sending of URLs and get handlers started'
url='https://gitlab.com/ubports/development/core/lomiri-url-dispatcher'
arch=(x86_64 i686 armv7h aarch64)
license=(LGPL)
conflicts=(url-dispatcher)
provides=(url-dispatcher)
depends=(lomiri-app-launch intltool apparmor)
makedepends=(glib2-devel glib2 git cmake cmake-extras dbus-test-runner lcov gcovr gmock)
checkdepends=(python-testtools python-nose python-dbusmock)
source=('https://gitlab.com/ubports/development/core/lomiri-url-dispatcher/-/archive/0.1.4/lomiri-url-dispatcher-0.1.4.tar.gz')
sha256sums=('SKIP')

# pkgver() {
#   cd ${_pkgname}
#   echo "r$(git rev-list --count HEAD).$(git describe --always)" | sed 's/\([^-]*-g\)/r\1/;s/-/./g'
# }

build() {
  cd ${_pkgname}-${pkgver}
  cmake -Denable_mirclient=off -DCMAKE_INSTALL_PREFIX:PATH=/usr -DCMAKE_INSTALL_LIBDIR="lib/" -DCMAKE_INSTALL_LIBEXECDIR="lib/" .
  make
}

package_lomiri-url-dispatcher() {
  cd ${_pkgname}-${pkgver}
  make DESTDIR="${pkgdir}/" install

}

package_lomiri-url-dispatcher-gui() {
  cd ${_pkgname}-${pkgver}
  make DESTDIR="${pkgdir}/" install
}
# vim:set ts=2 sw=2 et:
